import { LoginForm } from "@/components/forms/login-form"

export default function LoginPage() {
  return (
    <div className={"flex items-center justify-center h-[calc(100vh-10rem)] px-5"}>
      <LoginForm />
    </div>
  );
}
